/**
 * Cross-cutting application configuration.
 *
 * <p>Feature-specific configuration (e.g. a feature's own security rules,
 * its own beans) should live next to that feature. This package is for
 * things that apply to the whole application: JPA auditing, Jackson,
 * async executors, global CORS, etc.
 */
package br.com.hadryan.agro.manager.config;